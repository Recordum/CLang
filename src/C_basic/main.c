#include "pointer_typeCast.h"#include <stdio.h>#include <stdlib.h>#include "struct.h"#include <string.h>void pass_array(int arr[]) {    // int arr[] == int arr* 같은 의미 배열을 받을 때는 arr[]를 써서 명시 해주자    printf("배열 넘기기 %p\n", arr);    printf("배열 값 확인: %d\n", arr[3]);}void dynamic_allocation() {    Car *A = (struct Car*) malloc(sizeof(Car));    A->cost = 100;    strcpy(A->name, "myCar");    printf("Car 객체 Cost : %d\n", A->cost);    printf("Car 객체 name : %s\n", A->name);    printf("Car 객체 의 주소: %p\n", A);    free(A);}int* dangling_pointer_v1(void){    int x = 10;    return &x;}void just_call_by_value(int *x){    x = (int*)malloc(sizeof(int));    *x = 18;}int main(void){    int a =6;    just_call_by_value(&a);    printf("%d\n", a);}